---
description: UI and E2E spec structure, hooks, steps, tags, and spec helper policy
alwaysApply: true
---

# Test Structure and Tags

## Purpose

These rules define how UI and E2E specs must be structured in a universal Playwright + TypeScript testing framework.

The main goals are:

- keep UI and E2E tests readable;
- keep specs grouped by feature folder;
- make reports and traces easy to analyze;
- prevent hidden behavior in hooks;
- keep test scenarios explicit;
- standardize tags for CI filtering and reporting;
- avoid project-specific tag chaos;
- keep reusable technical helper logic out of specs.

Specs must live in feature folders. Do not add `tests/ui/*.ui.spec.ts` or `tests/api/*.api.spec.ts` at the layer root.

Rule reference:

- `.cursor/rules/spec-feature-placement.rules.mdc`

---

## Steps

- UI and E2E tests must use `test.step` for meaningful user-level phases.
- Step names must describe user intent.
- Step names must not describe low-level implementation details.
- Do not wrap every single Playwright action into a separate step.
- Do not put the whole test body into one generic step.
- Prefer 2-5 meaningful steps per UI or E2E test.
- Steps should normally represent phases such as navigation, user action, and verification.
- Steps should call Page Object or Component Object methods instead of exposing raw selector mechanics.

### Good

```ts
test("sorts products by price", { tag: ["@ui", "@smoke", "@sorting"] }, async ({ homePage }) => {
  await test.step("Open product catalog", async () => {
    await homePage.open();
    await expect(homePage.heading).toBeVisible();
  });

  await test.step("Apply ascending price sorting", async () => {
    await homePage.sorting.applySort("price,asc");
  });

  await test.step("Verify products are sorted by price", async () => {
    await expectSortedByPrice(await homePage.productList.getVisiblePrices(), "asc");
  });
});
```

### Bad

```ts
test("sorts products by price", { tag: ["@ui", "@smoke"] }, async ({ page }) => {
  await test.step("Click sort dropdown", async () => {
    await page.locator(".sort").click();
  });

  await test.step("Click price", async () => {
    await page.locator(".price").click();
  });

  await test.step("Click asc", async () => {
    await page.locator(".asc").click();
  });

  await test.step("Wait", async () => {
    await page.waitForTimeout(1000);
  });
});
```

### Why this is bad

This structure exposes implementation details instead of user intent.

It creates noisy reports and encourages low-level selector usage in specs.

---

## Step Naming Rules

Step names should be written from the user's perspective.

### Good step names

```text
Open product catalog
Apply ascending price sorting
Verify products are sorted by price
Filter products by brand
Submit checkout form
Verify order confirmation is shown
```

### Bad step names

```text
Click button
Fill input
Wait for table
Check element
Call method
```

### Rule

If a step name would not make sense to a product owner, QA reviewer, or support engineer, it is probably too technical.

---

## Hooks

- `beforeEach` may only perform safe navigation and page-loaded checks.
- `beforeEach` must not perform the action under test.
- `beforeEach` must not hide business flows or E2E journey steps.
- `beforeEach` must not silently change application state unless the state is generic setup required for every test in the suite.
- Do not repeat navigation already done in `beforeEach`.
- Keep navigation-path tests in a separate `describe` block when needed.
- Hook logic must stay small, explicit, and easy to reason about.
- If the same page-open plus loaded-marker setup is repeated across tests in one `describe`, move that shared setup to `beforeEach`.

For E2E specs, generic open or wait-for-ready setup in `beforeEach` is allowed.

Journey steps must remain visible in the spec and must not be hidden in `beforeEach`, other hooks, fixtures, Page Objects, or workflow wrappers.

### Good

```ts
test.describe("Product sorting", { tag: ["@ui", "@regression", "@sorting"] }, () => {
  test.beforeEach(async ({ homePage }) => {
    await homePage.open();
    await expect(homePage.heading).toBeVisible();
  });

  test("sorts products by price", async ({ homePage }) => {
    await test.step("Apply ascending price sorting", async () => {
      await homePage.sorting.applySort("price,asc");
    });

    await test.step("Verify products are sorted by price", async () => {
      await expectSortedByPrice(await homePage.productList.getVisiblePrices(), "asc");
    });
  });
});
```

### Bad

```ts
test.describe("Product sorting", { tag: ["@ui", "@regression", "@sorting"] }, () => {
  test.beforeEach(async ({ homePage }) => {
    await homePage.open();
    await homePage.sorting.applySort("price,asc");
  });

  test("sorts products by price", async ({ homePage }) => {
    await expect(homePage.productList.items).toBeVisible();
  });
});
```

### Why this is bad

The sorting action is the behavior under test, but it is hidden inside `beforeEach`.

The test body no longer clearly shows what scenario is being verified.

---

## E2E Spec Structure

E2E specs follow the same step and hook rules as UI specs, with additional constraints:

- the full user or business journey must remain visible in the spec;
- API or fixture setup may create preconditions only;
- setup must not replace the UI journey under test;
- final assertions must prove a meaningful journey outcome;
- do not hide the E2E flow inside fixtures, hooks, Page Object full-flow methods, API clients, helpers, or domain workflow wrappers.

Rule reference:

- `.cursor/rules/e2e-testing.rules.mdc`

---

## Authentication Setup

- Do not perform UI login in `beforeEach` unless the suite explicitly tests login or navigation behavior.
- For pre-authenticated UI tests, use the UI mode registered in the project map Auth Strategy (`storageState`, inject, or captured session).
- Unauthenticated specs and login-as-action specs must not inherit an authenticated `storageState`.
- Concrete users, roles, credentials, and login flows stay in the project layer.

Rule reference:

- `.cursor/rules/authentication-strategy.rules.mdc`

### Bad

```ts
test.beforeEach(async ({ loginPage, user }) => {
  await loginPage.open();
  await loginPage.login(user);
});
```

This is bad when the suite does not test login behavior.

### Better

```ts
test.describe("Authenticated product area", { tag: ["@ui", "@regression", "@auth"] }, () => {
  test.use({ storageState: "state/authenticated-user.json" });

  test("shows user-specific products", async ({ homePage }) => {
    await test.step("Open product catalog", async () => {
      await homePage.open();
      await expect(homePage.heading).toBeVisible();
    });
  });
});
```

---

## Navigation Rules

- If `beforeEach` opens a page, tests in that `describe` block should not repeat the same navigation.
- If a test verifies navigation itself, it should not reuse a `beforeEach` that already navigates to the destination.
- Navigation-path tests should be grouped separately.

### Good

```ts
test.describe("Product page content", { tag: ["@ui", "@regression", "@catalog"] }, () => {
  test.beforeEach(async ({ productPage }) => {
    await productPage.open();
    await expect(productPage.heading).toBeVisible();
  });

  test("shows product title", async ({ productPage }) => {
    await test.step("Verify product title is shown", async () => {
      await expect(productPage.title).toBeVisible();
    });
  });
});
```

### Good for navigation behavior

```ts
test.describe("Product navigation", { tag: ["@ui", "@regression", "@catalog"] }, () => {
  test("opens product details from catalog", async ({ catalogPage, productPage }) => {
    await test.step("Open product catalog", async () => {
      await catalogPage.open();
      await expect(catalogPage.heading).toBeVisible();
    });

    await test.step("Open first product details", async () => {
      await catalogPage.productList.openFirstProduct();
    });

    await test.step("Verify product details page is shown", async () => {
      await expect(productPage.title).toBeVisible();
    });
  });
});
```

---

## Spec Helper And Assertion Logic Policy

Specs should describe test scenarios, not host reusable technical logic.

Specs may contain:

- scenario data that is local to the spec;
- small deterministic constants;
- table-driven scenario cases;
- `test.step` blocks;
- calls to Page Object, Component Object, API client, builder, generator, or assertion helper APIs;
- direct scenario-specific assertions.

Specs must not accumulate non-trivial reusable technical logic.

Move technical logic out of specs when it includes:

- sorting or comparison algorithms;
- parsing logic used by assertions;
- normalization logic used by assertions;
- reusable predicate checks;
- reusable array, table, list, card, or response validation;
- large field-specific assertion branching;
- repeated helper functions used by multiple tests;
- local scenario orchestration such as `run*Scenario` or `execute*Flow`;
- stability polling, signature waits, or `expect.poll` synchronization helpers;
- reporting path builders such as `buildAllureSuitePath`;
- logic that makes the scenario hard to read.

Before creating helper logic, search existing helpers under `src/test/assertions/**`, `src/test/reporting/**`, and related project layers. Reuse or extend existing helpers instead of duplicating them in specs.

Rule reference:

- `.cursor/rules/spec-helper-policy.rules.mdc`

Preferred destinations:

- assertion helpers for reusable or non-trivial assertions;
- UI stability helpers under `src/test/assertions/ui/**` for poll/signature synchronization;
- reporting helpers under `src/test/reporting/**` for Allure metadata and suite path builders;
- test data datasets for reusable scenario cases;
- generators for unique primitive values;
- builders for reusable structured data;
- API clients only for reusable request composition;
- Page Objects or Components only for UI locators, actions, and state readers.

Assertion helpers may contain Playwright `expect`.

Page Objects and Component Objects must not contain Playwright `expect`.

Do not create helper files for one-line one-off logic.

Do not hide the user action under test inside helpers.

Do not move scenario data out of the spec when it is small, local, and improves readability.

Examples of logic that should usually move out of specs:

- monotonic sorting checks;
- price, name, date, or rating comparison helpers;
- text normalization used by assertions;
- parsing visible prices, dates, quantities, or ratings for assertion purposes;
- repeated validation of product cards, table rows, list items, or paginated responses;
- large `if/else` branching that chooses assertion behavior by field type.

### Good spec shape

```ts
test("sorts products by price", { tag: ["@ui", "@smoke", "@sorting"] }, async ({ productsPage }) => {
  await test.step("Open product catalog", async () => {
    await productsPage.open();
    await productsPage.waitForReady();
  });

  await test.step("Apply price ascending sorting", async () => {
    await productsPage.selectSort("price,asc");
  });

  await test.step("Verify visible products are sorted by price", async () => {
    await expectNumericValuesSorted(await productsPage.getVisibleProductPrices(), "asc");
  });
});
```

### Bad spec shape

```ts
function normalizeName(value: string): string {
  return value.trim().toLowerCase();
}

function isMonotonicAscending<T>(values: T[], compare: (left: T, right: T) => number): boolean {
  for (let index = 1; index < values.length; index += 1) {
    if (compare(values[index - 1], values[index]) > 0) {
      return false;
    }
  }

  return true;
}

test("sorts products by name", { tag: ["@ui", "@regression", "@sorting"] }, async ({ productsPage }) => {
  const names = await productsPage.getVisibleProductNames();
  const normalizedNames = names.map(normalizeName);

  expect(isMonotonicAscending(normalizedNames, (left, right) => left.localeCompare(right))).toBeTruthy();
});
```

### Why this is bad

The spec contains reusable normalization and comparison algorithms.

The test scenario is harder to read and the helper logic is harder to reuse or review.

---

## Tagging

- Use Playwright details-object tags.
- Do not put tags inside test title strings.
- Every focused UI test must be covered by `@ui`.
- Every E2E test must be covered by `@e2e`.
- Every UI and E2E test must be classified with an execution-scope tag: `@smoke` or `@regression`.
- `@e2e` replaces `@ui` as the layer tag for full-journey E2E specs by default.
- Do not tag full-journey E2E specs with `@ui` unless the project map explicitly requires both.
- Browser-based E2E may run in existing browser projects but must be tagged `@e2e` for reporting and filtering.
- Tags may be applied at `describe` level when they apply to all tests inside the suite.
- Test-level tags may add more specific classification.
- Do not introduce new tags without adding them to the tag registry or project map.

### Good

```ts
test.describe("Product sorting", { tag: ["@ui", "@regression", "@sorting"] }, () => {
  test("sorts products by price", async ({ homePage }) => {
    await test.step("Apply ascending price sorting", async () => {
      await homePage.sorting.applySort("price,asc");
    });
  });
});
```

### Good

```ts
test("shows login page for guest user", { tag: ["@ui", "@smoke", "@login"] }, async ({ loginPage }) => {
  await test.step("Open login page", async () => {
    await loginPage.open();
    await loginPage.waitForReady();
  });
});
```

### Good E2E

```ts
test(
  "completes registration and reaches authenticated account area",
  { tag: ["@e2e", "@regression", "@registration"] },
  async ({ registerPage, loginPage, accountPage }) => {
    // journey steps remain visible in the spec
  },
);
```

### Bad

```ts
test("shows login page @ui @smoke @login", async ({ loginPage }) => {
  await loginPage.open();
});
```

### Why this is bad

Tags inside title strings mix metadata with human-readable test names.

Structured tags are easier to validate, filter, and report.

---

## Tag Taxonomy

Use tags consistently by category.

### Required layer tag

Every focused UI test must have:

```text
@ui
```

Every full-journey E2E test must have:

```text
@e2e
```

Do not use both `@ui` and `@e2e` on the same full-journey E2E spec by default.

### Required execution-scope tag

Every UI and E2E test must have one of:

```text
@smoke
@regression
```

### Optional quality/type tags

Allowed examples:

```text
@visual
@a11y
@flaky
@wip
```

### Optional feature/domain tags

Feature/domain tags describe the product area, feature, or behavior under test.

Examples:

```text
@sorting
@filtering
@login
@register
@registration
@catalog
@contact
@checkout
@cart
@profile
@auth
```

Feature/domain tags are project-specific.

They are allowed only if defined in the tag registry or project map.

Use feature/domain tags when they add useful filtering, reporting, ownership, or traceability value.

Do not create feature/domain tags ad hoc in specs.

Do not use feature/domain tags as a replacement for required layer and execution-scope tags.

### Optional coverage-type tags for cross-browser and responsive

Use only when registered in the project map and explicitly planned in the feature or E2E journey plan.

Allowed examples when registered:

```text
@cross-browser
@responsive
```

Meaning:

- `@cross-browser` — test intent is cross-browser coverage for a documented browser-specific risk; it is **not** a browser name tag;
- `@responsive` — test intent is responsive or viewport-specific coverage for a documented layout or navigation risk; it is **not** a device or viewport size tag.

These tags classify coverage type.

They do not replace required layer tags (`@ui`, `@e2e`, `@visual`) or execution-scope tags (`@smoke`, `@regression`).

Do not use `@cross-browser` or `@responsive` on every test by default.

Use them only when the plan explicitly marks the scenario as cross-browser or responsive coverage.

### Forbidden browser, engine, and device tags

Browser, engine, viewport, and device names are execution environment details.

They are **not** Playwright test-intent tags.

Do not use tags such as:

```text
@chromium
@firefox
@webkit
@mobile
@tablet
@desktop
```

Browser and viewport selection belongs to:

- Playwright projects in `playwright.config.ts`;
- explicit viewport setup in the spec or project config when planned;
- Allure reporting metadata for reporting dimensions.

Do not encode browser or viewport in Playwright tags when a Playwright project or reporting metadata is the correct layer.

### Playwright projects vs tags

Playwright projects own browser and default viewport execution.

Tags own test intent, layer, execution scope, and optional coverage type.

Agents must use only Playwright projects documented in the project map.

Do not reference non-existing projects such as `ui-firefox` or `ui-mobile` until the project map registers them.

Current documented projects:

- `ui-chromium`
- `api`
- `e2e`

### Allure metadata for browser and viewport reporting

Allure metadata may describe reporting dimensions such as:

- browser or Playwright project;
- viewport;
- layer;
- feature;
- story;
- component;
- coverage type, such as cross-browser or responsive.

Allure metadata must not replace Playwright tags.

Playwright tags remain the source of truth for:

- test layer;
- smoke vs regression scope;
- optional coverage type when registered;
- feature/domain classification when registered.

Specs or approved reporting helpers may set Allure metadata.

Page Objects, Components, API clients, builders, and generators must not set Allure metadata.

Do not add Allure reporter integration, tokens, or credentials as part of tag policy changes.

Rule reference:

- `.cursor/rules/reporting-allure.mdc`

### Tag category examples

```ts
export const tags = {
  layer: ["@ui", "@api", "@e2e"],
  execution: ["@smoke", "@regression"],
  quality: ["@visual", "@a11y", "@flaky", "@wip"],
  coverageType: ["@cross-browser", "@responsive"],
  feature: ["@sorting", "@filtering", "@login", "@register", "@registration"],
  domain: ["@catalog", "@contact", "@checkout", "@cart", "@profile", "@auth"],
} as const;
```

These examples are illustrative.

The actual allowed tags must be documented by the project tag registry or project map.

---

## Smoke vs Regression Policy

- `@smoke` identifies a small, high-value subset for fast validation.
- `@regression` identifies broader coverage.
- A test should not be tagged with both `@smoke` and `@regression` unless the project explicitly requires duplicated classification.
- If smoke tests should also run during regression, configure the regression pipeline to include both `@smoke` and `@regression`.

For form or mutation UI features, smoke should include the **plain happy path** when that success path is in scope.

Plain happy path means required fields only and the core success confirmation.

Optional success variants such as attachment upload, optional checkboxes, or optional secondary fields belong in `@regression` by default.

Do not make an optional-path success the only smoke positive while the plain happy path is missing.

### Recommended CI filtering idea

```text
UI smoke run:
@ui AND @smoke

UI regression run:
@ui AND (@smoke OR @regression)

E2E smoke run:
@e2e AND @smoke

E2E regression run:
@e2e AND (@smoke OR @regression)
```

---

## Tag Registry Rule

Allowed tags must be documented in a tag registry or project map.

The registry should define:

- tag name;
- tag category;
- meaning;
- whether it is required or optional;
- whether it is universal or project-specific.

### Example registry

```ts
export const tags = {
  layer: ["@ui", "@api", "@e2e"],
  execution: ["@smoke", "@regression"],
  quality: ["@visual", "@a11y", "@flaky", "@wip"],
  coverageType: ["@cross-browser", "@responsive"],
  feature: ["@sorting", "@filtering", "@login", "@register", "@registration"],
  domain: ["@catalog", "@contact", "@checkout", "@cart", "@profile", "@auth"],
} as const;
```

Do not create ad-hoc tags in specs.

When a new feature/domain tag is needed:

- add it to the tag registry or project map first;
- explain its purpose;
- use it consistently across related specs.

---

## Anti-Patterns

Avoid:

- UI or E2E tests without `test.step`;
- one huge step wrapping the entire test;
- a separate step for every single click;
- technical step names such as `Click button` or `Fill input`;
- business actions hidden inside `beforeEach`;
- E2E journey steps hidden inside hooks, fixtures, or workflow wrappers;
- UI login in `beforeEach` for non-login tests;
- repeating navigation already performed by `beforeEach`;
- tags inside test title strings;
- untagged UI or E2E tests;
- UI tests without `@smoke` or `@regression`;
- E2E tests without `@e2e`;
- E2E tests without `@smoke` or `@regression`;
- tagging full-journey E2E specs with `@ui` by default;
- random project-specific tags not defined in the tag registry;
- mixing setup, action, and assertion in unclear steps;
- long helper functions inside specs;
- reusable parsing, normalization, sorting, comparison, or predicate logic inside specs;
- large assertion branching inside specs;
- feature/domain tags used without being registered;
- feature/domain tags used instead of required layer and execution-scope tags;
- browser, engine, or device names used as Playwright tags;
- viewport or device encoded as a tag instead of Playwright project or Allure metadata;
- `@cross-browser` or `@responsive` used without project map registration or explicit plan coverage;
- Allure metadata used as a substitute for required Playwright layer or execution tags.

---

## Main Principle

A UI or E2E spec should read like a clear user scenario.

Hooks prepare safe context.

Steps describe user intent.

Tags describe test intent, layer, execution scope, and optional registered coverage type.

Browser and viewport execution belongs to Playwright projects and Allure reporting metadata.

Scenario data may stay in the spec when it is local and readable.

Reusable technical helper logic belongs in assertion helpers, datasets, builders, generators, or other proper support layers.

Feature/domain tags may be used for reporting and filtering, but only when registered.

The test body must clearly show the behavior being verified.
