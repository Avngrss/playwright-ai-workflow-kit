---
description: page object pattern
alwaysApply: true
---

# Page Object Pattern

Each page must:

- represent one page only
- contain:
  - locators
  - actions
  - expectLoaded()

Rules:

- always create expectLoaded()
- never mix multiple pages
- never include navigation from other pages

Test must:

- call expectLoaded() after navigation
