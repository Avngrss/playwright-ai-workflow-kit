import { expect, test as base } from "./data.fixture";

/**
 * Page Object wiring layer.
 * Extend here with Page Object fixtures when implementation starts.
 *
 * Example (when justified):
 * export const test = base.extend<{ loginPage: LoginPage }>({ ... });
 */
export const test = base.extend({});

export { expect };
