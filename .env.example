# Starter environment template — provider-agnostic placeholders only.
# Copy to .env locally and fill values for your target application.
# Never commit .env or real secrets to the repository.
#
# Local .env corresponds conceptually to GitHub Actions configuration:
# - non-sensitive URLs → repository or environment Variables (vars.* in CI)
# - passwords, tokens, service credentials → Secrets (secrets.* in CI)
# See README.md and .cursor/rules/00-project-map.mdc for the CI vars/secrets model.
#
# Env names may be adapted per project, but update the project map and docs if changed.

# --- Feature Targets: simple project ---
#
# Simple baseline uses one UI target and one API target.
# Feature plans should still list only the targets used by each feature.

# UI/application target
UI_BASE_URL=https://practicesoftwaretesting.com/

# API/service target
API_BASE_URL=https://api.practicesoftwaretesting.com

# UI_PRECONDITION_API_BASE_URL is a legacy/simple-project shortcut only.
# It is not recommended for multi-target projects and is intentionally not active here.
# UI_PRECONDITION_API_BASE_URL=

# --- Feature Targets: additional examples (commented) ---
#
# Real projects register only the targets used by each feature in
# .cursor/rules/00-project-map.mdc. Names below are examples only.
# Optional setup/cleanup uses one of these registered targets; there is no global
# precondition URL model.
# CUSTOMER_APP_URL=
# ADMIN_APP_URL=
# CHECKOUT_APP_URL=
# AUTH_API_URL=
# USERS_API_URL=
# ORDERS_API_URL=
# PAYMENTS_API_URL=
# EXTERNAL_PARTNER_API_URL=

# --- Optional reporting ---

# Uncomment if you override the default Allure results directory locally.
# ALLURE_RESULTS_DIR=allure-results

# --- Secrets (local .env or CI Secrets — never commit real values) ---

# User passwords, API tokens, service account credentials, and TMS provider tokens
# belong in local .env (uncommitted) or GitHub Actions Secrets — not in this file.
# Do not add active token variable names to the committed template.
# TMS publishing to CI is not configured in the starter baseline.

# --- Optional TMS (comments only — no active provider tokens in repo) ---

# TMS tokens belong in user/global tool settings, local secret storage, or CI Secrets.
# Do not commit real TMS tokens to repository files.
# Qase is one supported example via user/global Cursor MCP when configured.
# Other TMS providers may be supported later through approved integration paths.
# CI must not publish TMS results unless explicitly planned and approved in the project map.
